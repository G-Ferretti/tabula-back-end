spring:
  datasource:
    username: admin
    password: admin
    url: jdbc:h2:mem:somedb
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true